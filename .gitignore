# Built packages - regenerated by ./package.sh
dist/

# The two original Inductive Automation Exchange resources, unpacked. Kept on disk
# as the reference this port was made against, but not redistributed here: they are
# IA's to publish, not ours. Re-download from the Ignition Exchange if you need them.
launchpad_oee.1.0.0/
launchpad_kpi.1.0.0/

# Gateway list / credentials for a deploy, if one is ever added here
*.local.sh
*.local.md
*.local.json

__pycache__/
*.pyc

# The two original Inductive Automation project exports, kept on disk as the
# reference this port was made against. Same reasoning as the unpacked copies
# above: they are IA's to publish, not ours. Re-download from the Ignition
# Exchange if you need them.
Example-OEE.zip
Example-KPI.zip

# The internal engineering record: what was wrong with the stock resource and how
# it was repaired, 52 items of it. Useful, and NOT for publication -- this is a
# port of somebody else's work, and a public repo says what the thing does, not
# where the original fell short.
docs/REPAIR.md
